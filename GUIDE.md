# Инструкция для команды GrippaNN-3 🚀

Привет! Никита (Team Lead) подготовил этот проект, чтобы мы могли работать быстро и без ошибок. Следуй этой инструкции по шагам.

---

## Шаг 1: Подготовка (сделай это один раз)

Установи эти программы, если их еще нет:
1. **Python 3.12** — [Скачать](https://www.python.org/downloads/) (при установке обязательно нажми галочку **"Add Python to PATH"**).
2. **Node.js** — [Скачать](https://nodejs.org/en/download/).
3. **Git** — [Скачать](https://git-scm.com/downloads).

---

## Шаг 2: Копирование проекта

Открой терминал (или PowerShell) и введи:
```bash
# Клонируем черновик для тренировки
git clone https://github.com/k4dzuma/GrippaNN-Draft.git
cd GrippaNN-Draft
```

---

## Шаг 3: Настройка на компьютере

### Для Бэкенда (Маша, Камилла, Ваня И.):
Запусти эти команды по очереди в папке `api`:
```bash
cd api
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env
python manage.py migrate
python manage.py create_admin
```
*Теперь твой бэкенд готов! Запуск: `python manage.py runserver`*

### Для Фронтенда (Даня, Серёжа):
Запусти эти команды в папке `app`:
```bash
cd app
npm install
```
*Теперь твой фронтенд готов! Запуск: `npm run dev`*

---

## Шаг 4: Как отправлять код (Автоматизация)

Никита создал скрипт, который сам всё копирует и отправляет на GitHub. Тебе не нужно вводить команды Git.

1. Найди в корне проекта файл **`start.bat`**.
2. Просто **дважды щелкни по нему**.
3. В открывшемся окне:
   - Выбери свой **номер** (например, 1 - Маша).
   - Введи **дату** (например, 25.02).
   - Выбери **куда пушить**:
     - Нажми `1` (**DRAFT**), если хочешь потренироваться и показать черновик Никите.
     - Нажми `2` (**STABLE**), если ты уверен, что код идеален и готов для основного проекта.
4. В конце нажми `y`, чтобы отправить код.

**Важно:** При самом первом пуше появится окно входа в GitHub — просто нажми "Sign in with browser" и подтверди вход.

---

## Шаг 5: После отправки

1. Зайди на GitHub в репозиторий, куда делал пуш.
2. Нажми зеленую кнопку **"Compare & pull request"**.
3. Напиши, что именно ты сделал, и нажми **"Create pull request"**.
4. Жди, пока Никита проверит и одобрит твой код!

---

*По всем вопросам пиши Никите в ЛС. Удачи!*
