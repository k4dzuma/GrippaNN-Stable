# Гайд по установке и работе (GrippaNN-3) 🚀

Привет! Этот гайд поможет тебе быстро настроить всё на своем компьютере и начать работу.

## 1. Что нужно установить (Must Have)

Чтобы проект работал, у тебя должны быть:

1. **Python 3.12+** (для бэкенда)
   - [Скачать Python](https://www.python.org/downloads/)
   - *Важно: при установке нажми галочку "Add Python to PATH"!*
2. **Node.js 20+** (для фронтенда)
   - [Скачать Node.js](https://nodejs.org/en/download/)
3. **Git**
   - [Скачать Git](https://git-scm.com/downloads)
4. **Visual Studio Code** (или любая другая IDE)

---

## 2. Настройка проекта

После установки всего вышеперечисленного, открой терминал (или PowerShell) и выполни:

## 1. Репозитории проекта

У нас теперь есть два репозитория:
1. **[DRAFT (Черновик)](https://github.com/k4dzuma/GrippaNN-Draft)** — Твоя песочница. Здесь тренируемся.
2. **[STABLE (Организация)](https://github.com/Bread7Team/GrippaNN-3)** — Основной проект. Сюда пушим финальный код.

### Клонирование (начни с Draft)
```bash
git clone https://github.com/k4dzuma/GrippaNN-Draft.git
cd GrippaNN-Draft
```

---

## 2. Авторизация на GitHub (Первый пуш)

Когда ты в первый раз запустишь `start.bat` и скрипт дойдет до отправки кода:
1. Появится всплывающее окно **Git Credential Manager**.
2. Нажми кнопку **"Sign in with your browser"**.
3. В открывшемся браузере нажми **"Authorize GitCredentialManager"**.
4. Всё, теперь твой компьютер "подружен" с GitHub, и пуши будут работать автоматически.

*Если возникнет ошибка "Permission denied", значит Никита еще не добавил тебя в список разработчиков (Collaborators). Напиши ему!*

### 2. Подготовка Бэкенда (Маша, Камилла, Ваня И.)
```bash
cd api
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env
python manage.py migrate
python manage.py create_admin
```

### 3. Подготовка Фронтенда (Даня, Серёжа)
```bash
cd app
npm install
```

---

## 3. Как работать (Автоматизация)

Мы используем скрипт `start.bat` в корне проекта. Он сам переносит твой код из папки `individual-parts` в проект.

### Твой ежедневный алгоритм:
1. Запусти `start.bat`.
2. Выбери свой номер (Маша - 1, Даня - 4...).
3. Выбери сегодняшнее число (например, `25.02`).
4. **ВЫБЕРИ КУДА ПУШИТЬ**:
   - Нажми `1`, если хочешь потренироваться в **DRAFT**.
   - Нажми `2`, если готов отправить финальную версию в **STABLE** (организация).
5. Подтверди пуш (`y`).
6. Твой код на GitHub. Создай Pull Request и жди одобрения Никиты.

---

## 4. Спец. задачи для DevOps (Ваня Бутузов)

Ваня, тебе нужно:
1. Установить **Docker Desktop** (обязательно).
2. Свой код за 25.02 (Dockerfile и docker-compose) ты пушишь также через `work.ps1` (выбор 6).
3. После твоего пуша проект должен запускаться командой: `docker-compose up --build`.

---

## 5. Важные правила

- **Не пушь напрямую в `main`** — у Никиты стоит защита. Всегда работай через свою ветку (скрипт сам её создает).
- **Сначала проверь, потом пушь** — перед запуском `work.ps1` убедись, что твой бэкенд или фронтенд запускаются.
- **Pull Request** — после каждого пуша заходи на GitHub и нажимай **Create Pull Request**, чтобы Никита мог одобрить твой код.

---

*По всем вопросам пиши Никите в ЛС! Удачной разработки!*
