# Инструкция для Team Lead: Как проводить Code Review и мержить PR

## 1. Когда приходит новый PR

### Первичная проверка (2 минуты)
1. Прочитать **название PR** — понятно ли, что было сделано?
2. Прочитать **описание** — есть ли описание изменений?
3. Проверить **ветку** — название соответствует `feat/...`, `fix/...`, `docs/...`?
4. Проверить **количество файлов** — слишком много файлов = плохой признак (PR должен быть атомарным)

### Что проверять в коде

#### Соответствие ТЗ
- Реализованы ли все поля из ТЗ (модели, сериализаторы)?
- Работает ли soft-delete (поле `deleted_at`, а не физическое удаление)?
- Соответствуют ли эндпоинты контракту API (`docs/api-contract.md`)?
- Правильные ли permissions (admin / hr_manager)?

#### Качество кода
- Нет ли закомментированного кода?
- Нет ли `console.log` / `print()` для отладки?
- Понятные ли названия переменных и функций?
- Нет ли дублирования кода?

#### Безопасность
- Нет ли паролей или секретов в коде (должны быть в `.env`)?
- Используется ли `Argon2` для хеширования паролей?
- JWT токены не хранятся в коде?
- Нет ли SQL-инъекций (должен использоваться ORM)?

#### Стиль
- Файлы на своих местах (модели в models/, views в views/ и т.д.)?
- Импорты организованы?
- Нет лишних пустых строк?

## 2. Как оставлять комментарии

### Хорошие комментарии
```
# Конструктивные, с предложением решения:

"Здесь нужно добавить проверку deleted_at — иначе удалённые записи тоже будут возвращаться.
Предлагаю добавить .filter(deleted_at__isnull=True) в get_queryset."

"Это поле должно быть обязательным по ТЗ. Добавь required=True."

"Отличное решение с миксином для истории! 👍"
```

### Плохие комментарии
```
# НЕ надо так:
"Переделай"
"Не нравится"
"Почему так?"
```

## 3. Когда принимать PR (Approve)

Принимаем, если:
- [ ] Код соответствует ТЗ
- [ ] Нет ошибок в логике
- [ ] Нет проблем с безопасностью
- [ ] Нет закомментированного кода и отладочных выводов
- [ ] Файлы в правильных папках
- [ ] Коммиты осмысленные (Conventional Commits)

## 4. Когда отправлять на доработку (Request changes)

Отправляем, если:
- Не реализована часть ТЗ
- Есть баги или ошибки в логике
- Пароли или секреты в коде
- Физическое удаление вместо soft-delete
- Нет обработки ошибок на важных участках
- Неработающий код (ошибки импорта, синтаксиса)

**Всегда указывай конкретно**, что нужно исправить и почему.

## 5. Как мержить (Squash Merge)

1. Убедиться, что PR одобрен (есть ваш Approve)
2. Нажать **"Squash and merge"** (НЕ обычный merge)
3. Отредактировать сообщение коммита — должно быть осмысленным:
   ```
   feat: CRUD для организаций — модели, сериализаторы, views, urls (#3)
   ```
4. Подтвердить merge
5. **Удалить ветку** после мержа (кнопка "Delete branch")

### Почему Squash Merge?
- Все коммиты из ветки объединяются в один
- История в main остаётся чистой и линейной
- Каждый коммит в main = одна законченная фича

## 6. Порядок мержа (рекомендуемый)

```
1. feat/project-setup     (TL — настройка проекта)
2. feat/core-models       (Backend-1 — модели организаций, отделов, должностей)
3. feat/crud-org          (Backend-1 — CRUD организаций)
4. feat/crud-departments  (Backend-1 — CRUD отделов)
5. feat/crud-positions    (Backend-1 — CRUD должностей)
6. feat/employee-models   (Backend-2 — модели сотрудников, файлов)
7. feat/crud-employees    (Backend-2 — CRUD сотрудников)
8. feat/file-upload       (Backend-2 — загрузка файлов)
9. feat/hr-operations     (Backend-3 — кадровые операции)
10. feat/change-history   (Backend-3 — история изменений)
11. feat/jwt-auth         (Backend-3 — JWT авторизация, пользователи)
12. feat/frontend-setup   (Frontend-1 — настройка React)
13. feat/frontend-refs    (Frontend-1 — страницы справочников)
14. feat/frontend-emp     (Frontend-2 — страницы сотрудников, операций, истории)
```

## 7. Если main сломан

```bash
# Проверить, что main работает
git checkout main
git pull origin main
cd api && python manage.py migrate && python manage.py runserver
# Если ошибка — найти виновный PR и откатить или исправить
```

## 8. Чеклист перед финальной сдачей

- [ ] Все модели из ТЗ реализованы
- [ ] Все CRUD работают через API
- [ ] JWT авторизация работает (login → access + refresh)
- [ ] Soft-delete на всех сущностях
- [ ] История изменений записывается
- [ ] Ролевая модель работает (admin / hr_manager)
- [ ] Frontend отображает все разделы
- [ ] Файлы загружаются и отображаются
- [ ] README.md актуален
- [ ] Нет секретов в коде
